{
    "description": "Represents any object where we can subscribe on",
    "type": "object",
    "properties": {
        "path": {"type":  "string", "description": "The path where to publish"},
        "output_schema": {}
    },
    "required": ["path", "output_schema"]
}