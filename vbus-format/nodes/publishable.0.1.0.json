{
    "description": "Represents any object where we can subscribe on",
    "type": "object",
    "properties": {
        "path": {"type":  "string", "description": "The path where to subscribe"},
        "input_schema": {}
    },
    "required": ["path", "input_schema"]
}